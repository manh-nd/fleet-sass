plugins {
    id 'java-library'
}

dependencies {
    // Domain has zero external runtime dependencies — pure Java only.
    // Lombok is an annotation processor (compile-time only) — not a runtime dependency.
}